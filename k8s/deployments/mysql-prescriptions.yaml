apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-prescriptions
  namespace: nephro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-prescriptions
  template:
    metadata:
      labels:
        app: mysql-prescriptions
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: root-password
            - name: MYSQL_DATABASE
              value: prescriptions_db
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-prescriptions
  namespace: nephro
spec:
  selector:
    app: mysql-prescriptions
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
